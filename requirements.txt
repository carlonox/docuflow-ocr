# ------------------------------------------------------------------
# Core
# ------------------------------------------------------------------
numpy>=1.24
Pillow>=10.0

# OCR providers (install what you use)
# surya-ocr>=0.5          # accurate provider (CPU, ~4GB RAM)
# rapidocr-onnxruntime>=1.3  # fast provider (ONNX, no Paddle)
# google-cloud-vision>=3.7   # cloud provider
# requests>=2.31          # Ollama provider + LLM corrector

# Optional: advanced preprocessing (Sauvola binarization, deskew)
# scikit-image>=0.21

# Optional: legacy fallback
# pytesseract>=0.3.10     # + system tesseract binary

# Optional: spreadsheet integration
# gspread>=5.10           # Google Sheets
# google-auth>=2.23

# Optional: browser automation
# selenium>=4.15

# Development / tests
pytest>=7.0
